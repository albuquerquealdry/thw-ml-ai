---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: observability
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`grafana.10.0.0.180.nip.io`)
      kind: Rule
      services:
        - name: grafana
          port: 80
